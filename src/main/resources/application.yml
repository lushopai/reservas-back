spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reservas?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Santiago
    username: root
    password: 2497
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: false

server:
  port: 8080

jackson:
  serialization:
    write-dates-as-timestamps: false
  time-zone: America/Santiago
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG

app:
  reservas:
    anticipacion-minima-horas: 24
    estadia-minima-dias: 1
    estadia-maxima-dias: 30
    servicio-anticipacion-minima-horas: 2
  precios:
    iva-porcentaje: 0.19
    descuento-vip-porcentaje: 0.05